# Pending uploads

Raw art-bible sheets that were uploaded but not yet cut into game sprites
(the session's image-read budget was exhausted before they could be viewed).
Preserved here so no data is lost. In a fresh session, open each, identify
the character/enemy, and cut its animation-row poses into `../assets/`
(the enemy/boss animation variants auto-load: `<key>`, `<key>_run`,
`<key>_hurt`, `<key>_attack`).
