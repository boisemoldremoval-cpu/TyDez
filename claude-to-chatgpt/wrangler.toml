name = "claude-to-chatgpt"
main = "cloudflare-worker.js"
compatibility_date = "2024-09-23"

# Set the Anthropic key as a secret for closed-proxy mode:
#   wrangler secret put ANTHROPIC_API_KEY
# Otherwise the worker uses each request's `Authorization: Bearer` header.
